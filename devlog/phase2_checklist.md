# Phase 2 检查清单 — 控制流

从《Meow 语言基础语法设计与规范》第 5 章提取的语法规则与语义行为，逐项验证。

---

## 5.1 条件判断

| 编号 | 规则 | 规范描述 | 测试状态 |
|------|------|----------|----------|
| CF-01 | 基本 `if` | `if 条件` 进入分支 | ✅ 已覆盖 |
| CF-02 | `if-else` | 条件为假时走 else 分支 | ✅ 已覆盖 |
| CF-03 | `if-elif-else` 链 | 多条件分支链 | ✅ 已覆盖 |
| CF-04 | 嵌套 `if` | if 内部嵌套 if | ✅ 已覆盖 |
| CF-05 | `true` 作为条件 | `if true` 进入分支 | ✅ 已覆盖 |
| CF-06 | `false` 作为条件 | `if false` 不进入分支 | ✅ 已覆盖 |
| CF-07 | `null` 作为条件 | `if null` 不进入分支（falsy） | ✅ 已覆盖 |
| CF-08 | `0` 作为条件 | `if 0` 不进入分支（falsy） | ✅ 已覆盖 |
| CF-09 | 非零数字作为条件 | `if 42` 进入分支（truthy） | ✅ 已覆盖 |
| CF-10 | 空字符串 `""` 作为条件 | `if ""` 不进入分支（falsy） | ⬜ |
| CF-11 | 非空字符串作为条件 | `if "hi"` 进入分支（truthy） | ⬜ |
| CF-12 | 空列表 `[]` 作为条件 | `if []` 不进入分支（falsy） | ⬜ |
| CF-13 | 非空列表作为条件 | `if [1]` 进入分支（truthy） | ⬜ |
| CF-14 | 复杂条件表达式 | `and` / `or` / `not` 组合条件 | ⬜ |
| CF-15 | if-elif-else 全分支覆盖 | 每个分支都有对应输入触发 | ⬜ |
| CF-16 | 嵌套 if（缩进层级 ≥ 3） | 三层以上嵌套 | ⬜ |
| CF-17 | if 块内新建变量，块外不可见 | 块级作用域验证 | ⬜ |
| CF-18 | 仅 `if-elif` 无 `else` | 无 else 时条件全不满足则跳过 | ⬜ |

## 5.2 循环

### for 循环

| 编号 | 规则 | 规范描述 | 测试状态 |
|------|------|----------|----------|
| CF-19 | `for x in list` 遍历列表 | 基本列表遍历 | ✅ 已覆盖 |
| CF-20 | `for x in range n` | range 单参数遍历 | ✅ 已覆盖 |
| CF-21 | `for x in range a, b` | range 双参数遍历 | ✅ 已覆盖 |
| CF-22 | 遍历空列表 | `for x in []` 不执行循环体 | ⬜ |
| CF-23 | 遍历单元素列表 | 列表只有一个元素 | ⬜ |
| CF-24 | for 内部使用 `break` | break 跳出循环 | ✅ 已覆盖 |
| CF-25 | for 内部使用 `continue` | continue 跳过当次 | ✅ 已覆盖 |
| CF-26 | 嵌套 for 循环 | 外层内层双重循环 | ✅ 已覆盖 |
| CF-27 | for 循环变量块外不可见 | 循环变量作用域验证 | ⬜ |
| CF-28 | for 内 break 跳出内层循环 | 嵌套循环中 break 只跳出当前层 | ⬜ |

### while 循环

| 编号 | 规则 | 规范描述 | 测试状态 |
|------|------|----------|----------|
| CF-29 | 基本 `while` | 条件为真时循环执行 | ✅ 已覆盖 |
| CF-30 | 条件初始为 false | `while false` 零次执行 | ⬜ |
| CF-31 | `while true` + break | 无限循环 + break 安全退出 | ✅ 已覆盖 |
| CF-32 | while 内部使用 `continue` | continue 跳过当次 | ✅ 已覆盖 |
| CF-33 | while 循环内修改循环变量 | 变量递增/递减控制循环 | ✅ 已覆盖 |
| CF-34 | while 条件为复合表达式 | `while a > 0 and b > 0` | ⬜ |

### break / continue

| 编号 | 规则 | 规范描述 | 测试状态 |
|------|------|----------|----------|
| CF-35 | `break` 立刻跳出整个循环 | 跳出当前循环 | ✅ 已覆盖 |
| CF-36 | `continue` 跳过本次循环剩余部分 | 直接开始下一次循环 | ✅ 已覆盖 |
| CF-37 | break 在嵌套循环中只跳出当前层 | 外层循环不受影响 | ⬜ |
| CF-38 | continue 在 while 中的行为 | 跳回条件判断 | ✅ 已覆盖 |

---

## 统计

- 总计：38 项规则
- 已覆盖：18 项
- 未覆盖：20 项
- 覆盖率：47.4%
